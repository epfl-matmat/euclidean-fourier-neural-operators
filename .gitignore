# Julia
# Manifest.toml is committed on purpose (pins exact versions for reproducibility);
# delete it if you want Pkg to resolve fresh versions
*.jl.*.cov
*.jl.cov
*.jl.mem
/docs/build/
/dev/

# Data (obtain from Phaidra, see README.md and data/README.md)
data/mlrpa_dataset.jld2
data/MLPOT
MLPOT

# Outputs (figures/ is committed on purpose — paper artifacts + README hero)
results/
runs/

# WandB
wandb/
.env

# OS / editors
.DS_Store
*.swp

# Wandb.jl Python environment (created on demand)
.CondaPkg/
